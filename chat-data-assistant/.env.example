# ── 数据库配置 ──
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_db
DB_USER=read_only
DB_PASSWORD=

# ── LLM 配置（留空则需在页面配置区填写）──
LLM_API_KEY=
LLM_PROVIDER=openai
LLM_BASE_URL=
LLM_MODEL=
LLM_TEMPERATURE=0.2
LLM_MAX_TOKENS=2048

# ── 调试：true 时把实际执行的 SQL 写入 logs/sql_debug.log ──
DEBUG_SQL=false
