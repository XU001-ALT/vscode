
import streamlit as st


def render_settings():
    st.markdown("## Settings")
    st.info(
        "Existing configuration functions remain unchanged. "
        "This page will contain API, model, database and schema settings."
    )
