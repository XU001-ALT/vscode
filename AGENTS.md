# SAF original Streamlit website

Follow `D:/Codex/AGENTS.md`.

- Preserve the original five-page website; do not add model prediction or training modules.
- Keep upstream application files byte-identical where possible and document local adaptations.
- Keep runtime data, PDFs, credentials and virtual environments out of Git.
- Use an independent virtual environment and port 8502 by default.
- Verify source hashes, five-page Streamlit AppTest coverage, compilation, dependency consistency and HTTP startup.
